#ifndef SCHEDULER_H__
#define SCHEDULER_H__

class scheduler
{
public:
	// construct & destruct
	scheduler();
	~scheduler();
	// plugin func's
	void init(void);
	void addfx(void);
	void quit(void);
private:
	// plugin var's
	// ...
};

#endif

